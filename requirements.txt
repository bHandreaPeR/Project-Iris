pandas>=2.0,<3.0
numpy>=1.24,<2.0
yfinance>=0.2.40,<0.3
requests
python-dotenv
APScheduler>=3.10,<4.0

# ML
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0
lightgbm>=4.0              # LightGBM ensemble (blended with XGBoost)
optuna>=3.0                # Hyperparameter tuning (optional, --tune flag)
shap>=0.44,<1.0
scipy>=1.11,<2.0

# NLP / sentiment
transformers>=4.35,<5.0
torch>=2.0                 # CPU only is fine for FinBERT inference
vaderSentiment>=3.3        # fallback if torch unavailable

# Reporting / viz
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0

# Utilities
lxml>=4.9                  # HTML parsing (SEC EDGAR index pages)
tqdm>=4.0
joblib>=1.3
pyarrow>=14.0              # parquet I/O for panel cache
